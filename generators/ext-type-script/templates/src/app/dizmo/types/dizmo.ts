/* tslint:disable:interface-name no-empty-interface */
import { Dizmo as IDizmo } from '@dizmo/types';

export interface Dizmo extends IDizmo {

    //
    // @info: extend if required (or desired)
    //

}

export default Dizmo;
